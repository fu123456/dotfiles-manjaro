;; {{{ ag config
;; to see @ https://agel.readthedocs.io/en/latest/configuration.html#highlighting-results
(require 'ag)
(setq ag-highlight-search t)
(setq ag-reuse-window 't)
(setq ag-executable "/usr/bin/ag")
(setq ag-reuse-buffers 't)
;; }}}
